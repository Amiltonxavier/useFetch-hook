import styled from "styled-components";

const container = styled.div`
  height: 100vh;
  margin-left: auto;
  margin-right: auto;
  width: 100%;
  display: flex;
  align-items: center;
`;

export const s = { container };
